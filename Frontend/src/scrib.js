// uses the input credentials to get user information and 
// opens the web app containing the appropriate information
function sign_in() { console.log("Foo!!!!!!!!!!"); }

 // this function lets the back-end use the provided credentials 
 // to create a new account within the database
function sign_up() { console.log("I Can't Get No Satisfaction"); }